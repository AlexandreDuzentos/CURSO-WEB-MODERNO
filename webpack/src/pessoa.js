/**
 * Usando recursos do ECMASCRIPT 2015, como classes e um padrão do sistema de módulos do
 * ECMASCRIPT para exportar coisas para fora do módulo.
 * 
 * O código abaixo não será interpretado nativamente pelo módulo, pois embora ele já suporte
 * o sistema de módulos do ECMASCRIPT 2015, para usá-lo é preciso fazer algumas configurações.
 * 
 * */

export default class Pessoa {
   cumprimentar(){
      return "Bom dia"
   }
}

