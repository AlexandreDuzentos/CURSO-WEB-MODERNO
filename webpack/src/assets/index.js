// importando um arquivo css que se deseja que o webpack enchergue, ou seja, que caia no radar dele
import "./css/estilo.css"

// importando um arquivo css que se deseja que o webpack enchergue, ou seja, que caia no radar dele
import "./scss/index.scss"